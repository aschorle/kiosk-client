# Installer

Der Installer kennt nur den Appliance-Pfad.

Einstieg:

```bash
sudo KIOSK_USER=rock ./installer/install.sh
```

Aktive Module:

- `install-common.sh`
- `install.sh`
- `install-radxa.sh`
- `install-rpi.sh`
- `appliance.sh`
- `packages.sh`
- `verify.sh`
- `runtime.sh`
- `tty.sh`
